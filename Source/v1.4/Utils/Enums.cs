using System;

namespace ATReforged
{
    public enum ATR_OptionsTab { General, Security, Connectivity }

    public enum ATR_ServerType
    {
        None = 0,
        SkillServer,
        SecurityServer,
        HackingServer
    }
}
